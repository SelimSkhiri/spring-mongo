version: "3.8"

services:
  
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    ports:
      - 8080:8080
    depends_on: 
      - db  
  db:
    image: mongo:latest
    container_name: db
    ports:
      - 27017:27017